spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/api-person
    username: postgres
    password: banco123
    jpa:
      show-sql: true
      hibernate:
        ddl-auto: update
      properties:
        hibernate.format_sql: true

